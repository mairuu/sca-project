[defaults]
inventory         = inventory.ini
# remote_user is injected at runtime via ansible_user in inventory (from Jenkins builder-ssh-key credential)
host_key_checking = False
roles_path        = roles/
stdout_callback   = yaml
